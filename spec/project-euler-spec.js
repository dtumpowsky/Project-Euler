import { Fibonacci } from './../src/project-euler';

describe('Thing', function() {
  // it('should correctly determine whether three sides are not a triangle', function() {
  //   var prob = new Fibonacci(0)
  //   expect(prob.problem2()).toEqual("4000000");
  // });
  it('is the easiest spec ever', function() {
    expect(1).toEqual(1);
  });
});
